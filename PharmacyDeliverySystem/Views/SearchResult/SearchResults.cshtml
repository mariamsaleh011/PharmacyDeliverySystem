@model List<Product>

<h2>نتائج البحث</h2>

@if (!Model.Any())
{
    <p>لا يوجد نتائج.</p>
}
else
{
    foreach (var p in Model)
    {
        <div class="product-card">
            <h3>@p.Name</h3>
            <p>@p.Description</p>
            <p>@p.DrugType</p>
        </div>
    }
}
